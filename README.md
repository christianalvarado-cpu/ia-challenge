# ia-challenge

Proyecto final en preparacion.

Este repositorio construira un dataset analitico llamado `comercial` a partir de las tablas `customers`, `products`, `sales` y `shops`, y generara un dashboard HTML local.

La documentacion inicial esta en `docs/`.
